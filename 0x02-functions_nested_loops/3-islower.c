#include "main.h"

/**
 * _islower - Checks for lowercase characters
 *
 * @c: ASCII Character
 *
 * Return: 0
 */

int _islower(int c)
{
	if (_islower(c))
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
