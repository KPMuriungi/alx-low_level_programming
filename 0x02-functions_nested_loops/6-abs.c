#include "main.h"

/**
 * _abs - Computes the absolute value of an integer
 *
 * @i: Parameter
 *
 * Return: 0
 */

int _abs(int i)
{
	if (i >= 0)
	{
		return (i);
	}
	else
	{
		return ((-1) * i);
	}
}
